# CONTEXT

這個專案的領域語彙。輸出裡提到領域概念時，用這裡定義的詞，不要漂移到別的同義詞。

## 這是什麼

**一個給單一使用者的 SD-IL（Illustrious）txt2img 提詞編輯器，以 ComfyUI 節點作為載體。**

不是字串工具包。`STRING` 輸出是出口，不是目的。產品是「把提詞拆成 chip、分群、調權重、重排」這個編輯行為，加上手工累積的中英標籤庫。

範圍界線：個人使用，不公開發佈，只服務一條 txt2img 工作流。見 [ADR-0001](docs/adr/0001-product-is-a-prompt-editor.md)。

## 詞彙

### 區塊 (Block)

提詞的一個**具名語意分群** — 「品質」「角色」「服裝」「場景」之類。一個區塊裝一串 chip。

區塊有：名字（自由文字）、順序、on/off 開關。關掉的區塊保留內容但不進輸出。

**不要叫它「輸入框」或 `string_N`。** 那是舊實作的痕跡：十個無名的槽位，語意只存在使用者腦中。改成「區塊」是刻意的 — 把 chip 拖到另一個區塊是**重新分類**，不是移動位置。見 [ADR-0002](docs/adr/0002-prompt-state-as-single-json-widget.md)。

### Chip

提詞裡的**一個標籤，作為離散物件存在** — 不是字串的一段。帶有：英文標籤文字、權重、（若標籤庫查得到）中文翻譯。

Chip 模型本身就消除了手打字串的兩類垃圾：連續逗號、殘留空白。工具不另外做正規化或去重（見「刻意不做的事」）。

### 權重 (Weight)

Chip 的強度，序列化為 ComfyUI 慣例的 `(text:1.1)`。權重為 1.0 時輸出原始文字，不加括號。

### 標籤庫 (Tag Library)

`js/tags.json` — 3981 筆手工累積的 `{en, zh, cat}`，涵蓋 134 個兩層分類。自動完成的資料來源，同時提供 chip 上顯示的中文翻譯。

**凍結資產**：沒有產生器，缺標籤時手動補一筆。見 [ADR-0006](docs/adr/0006-tag-library-is-a-frozen-handmade-asset.md)。

### 分類 (Category)

標籤庫裡的兩層分類法（`人物/眼睛`、`服飾/上衣`、`表情動作/手部動作`…）。

**分類不等於區塊。** 這兩個詞很容易混用，但它們是不同的東西：分類是標籤**本身**的屬性，由標籤庫定義；區塊是**這一張圖**的組織方式，由使用者定義。兩者對不起來 —`masterpiece` 的分類是「畫面/藝術風格」，但使用者心中它屬於「品質」區塊；`fat, bbw, plump` 在使用者心中是一群，在分類法裡散落各處。

用分類自動把 chip 歸位到區塊是**已知的未來可能**，但目前刻意不做。

### 範本 (Template)

一組可重用的**區塊串列**。長度 1 就是單一區塊的範本（例如常用角色），長度 N 就是整套提詞的範本（例如固定的負面提詞）。存在 `templates.json`（gitignore，個人資料）。

一個資料結構同時涵蓋兩種粒度，不區分「區塊範本」和「整套範本」。

### 正面提詞 / 負面提詞

各自是**一個獨立的節點實例**，不是同一個節點的兩個區。兩者需求完全相同（都要 chip、權重、分組），所以是同一個節點類型用兩次。

### SD-IL

Illustrious 系的 SDXL 模型（本專案的實際對象是 `waiNSFWIllustrious_v150`）。使用 Danbooru 標籤體系，這決定了標籤庫的形狀。

## 刻意不做的事

記下來，避免日後有人「順手補上」：

- **不做去重或正規化** — 底線 vs 空格的同義重複（`boku_no_hero_academia` vs `boku no hero academia`）留給使用者自己判斷。跨區塊的重複有時是刻意的強調。
- **不用分類法自動歸位 chip** — 見上面「分類」條目。
- **不公開發佈** — 不加 LICENSE、打包 metadata、向後相容承諾。
- **不維護標籤庫的產生流程** — 見 ADR-0006。
- **不做 chip 層級的 on/off** — 單顆不要就刪掉；「整群一起試」才是實際的操作。
- **`delimiter` / `remove_empty` 保留** — 沒有造成問題，不值得為了刪而刪。

## 環境

- ComfyUI `0.31.1` / comfyui-frontend-package `1.48.7`
- 工作流 `user/default/workflows/SD-IL_basic.json` 使用**普通 `CLIPTextEncode`**，非 SDXL 變體
- 本機參考實作：`rgthree-comfy` 的 Power Lora Loader（單一 widget 存結構化串列）、`ComfyUI-MiniMax-H3`（`addDOMWidget` 用法）
